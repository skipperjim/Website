extends layout

block content
    div.container
        h1 You gotta have administrator rights to view this page, bro.
        if(!user)
            include login
        if(user)
            p You are currently logged in as #{user.username}
            a(href="/logout") Logout